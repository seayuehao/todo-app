services:
  api:
    build: .
    container_name: todo-api
    ports:
      - "8091:8091"
    environment:
      GIN_MODE: release
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped